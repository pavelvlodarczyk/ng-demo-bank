import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-bank-child',
  imports: [RouterLink],
  templateUrl: './child.html',
  styleUrl: './child.scss',
})
export class Child {

}
